package main

import (
	"fmt"
 "strings"
)

func main(){
    var name string
    name = "GoLang"
    fmt.Println("Lowercase:", strings.ToLower(name))
    fmt.Println("Uppercase:", strings.ToUpper(name))
};
